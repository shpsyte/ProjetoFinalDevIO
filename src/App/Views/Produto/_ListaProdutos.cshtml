@model IEnumerable<App.ViewModels.ProdutoViewModel>
 
<table class="table table-hover">
    <thead class="thead-dark">
        <tr>
             <th>
                @Html.DisplayNameFor(model => model.ImageUpload)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
           
            <th>
                @Html.DisplayNameFor(model => model.Fornecedor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    @foreach (var item in Model) {
        <tr>
            <td>
                <img width="100px" height="100px" class="img-fluid" src="~/images/@item.Image" />
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            
            <td>
                @Html.DisplayFor(modelItem => item.Fornecedor.Name)
            </td>
             <td class="text-right">
                <a asp-action="Details" asp-controller="Produto" asp-route-id="@item.Id" class="btn btn-info"><i class="fa fa-search"></i></a> 
                <a asp-action="Edit" asp-controller="Produto" asp-route-id="@item.Id" class="btn btn-warning"><i class="fa fa-pencil-alt"></i></a> 
                <a supress-by-claim-name="produto" supress-by-claim-value="del" asp-action="Delete" asp-controller="Produto" asp-route-id="@item.Id" class="btn btn-danger"><i class="fa fa-trash"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
